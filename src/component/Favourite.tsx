import React from "react";

const Favourite: React.FC = () => {
//   const dispatch = useDispatch();
//   const fav = useSelector((state: RootState) =>
//     state.products.cart.filter((item) => item.quantity > 0)
//   );

  return (
    <div>
      <h2>Favourite</h2>
    </div>
  );
};

export default Favourite;
